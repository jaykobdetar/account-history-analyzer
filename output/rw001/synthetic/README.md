# RW-001 public presentation fixtures

RW-001 PUBLIC PRESENTATION FIXTURE: sensitivity outcomes were manually constructed after processing artificial source records. These are renderer regression cases, not optimizer output, numerical validation, or authorship truth.

Source inputs contain only newly generated synthetic records. Case labels are read after source processing and never enter feature calculations. The candidate/status facts deliberately replace selected exported sensitivity observations. Therefore these documents exercise presentation and JSON reload behavior; they must not be submitted as numerical-oracle or production recomputation evidence. Actual full analysis/recompute checks are recorded separately.
