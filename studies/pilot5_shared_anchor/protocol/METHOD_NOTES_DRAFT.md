# Pilot 5: exploratory shared-anchor chronology — method notes

These notes precede final source construction and are not an execution registration. The final Pilot5 construction rules, selected inputs, environments and outcomes must be bound before real scores. Pilot4's methods, failures, empty cohort, and conclusion remain preserved; Pilot5 is a separately labeled exploratory design.

The requested evaluation uses five source samples to form four histories sharing one early anchor: continuity or a constructed source-account switch, each with or without a community change. Every history must execute the ordinary full AHAS 1.0.4 analysis pipeline. Pilot4's empty paired cohort does not substitute for the requested Pilot5 execution. Source selection will use only the existing audited survivors and registered metadata rules; a final construction rule will specify exactly which five samples and volumes are retained before scoring.

This is one common-anchor experimental unit with four related outcomes. The design is nonreciprocal: it does not rotate across all source-account/community anchors. Repeated anchor text and reused source accounts do not provide independent samples or four independent replications. Prior Pilot4 feasibility, date/volume and audit exposure must be disclosed. These samples are not a fresh held-out cohort selected without prior source knowledge.

Keep the unchanged analyzer, ordinary full pipeline, pooled-comments primary surface/function-word temporal method and its window/search settings. Retain all canonical outputs privately. Select primary saved windows by the exact ordered IDs in the primary summary, since sensitivity windows can share the stream ID. Sensitivity output cannot replace the primary result. Construction details and volume controls are Pilot5-specific and must not be described as satisfying Pilot4's failed gates unless independently demonstrated.

The localization tolerance remains **ten supplied-record positions, inclusive**. Report any candidate occurring anywhere separately from a candidate matching the constructed junction. Keep the legal-window grid, best attainable error and temporal resolution distinct from achieved localization. A construction junction in a continuity history is a diagnostic seam, not positive source-switch truth. Source-account labels remain proxies and do not establish human authorship or takeover events.

Report availability and reasons for each of the four full histories. A successfully executed constant series is an observed zero-candidate outcome. An unavailable or failed full analysis retains null candidate and localization outcomes; missing results are not zeros. Record grid attainability separately where bound metadata supports it. For executed switches, report matching within ten positions, exact containment as descriptive information, nearest-candidate error and excess over the best legal grid. Report continuity-seam alignment separately.

Use descriptive per-history results and explicitly paired condition differences on the common anchor only. With one unit, do not supply IID confidence intervals, a bootstrap, significance claims, population accuracy or causal claims about community or source identity. State the date and volume mismatch that remains in the actual five samples. The final report must describe the real execution results and failures with their resource receipts, rather than stopping at another feasibility-only summary.
