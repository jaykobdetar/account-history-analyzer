# Chronological preparation — verified, not scored

Four source-account pairs were prepared from eight additional accounts. The registered target was six pairs from twelve accounts; the Cornell slots remain unfilled because all seven metadata-eligible candidates shared known threads with selected confirmation records. No replacement accounts, partial-history purges or relaxed guards were used.

| Home community | Selected source accounts / planned | Formed source pairs / planned |
|---|---:|---:|
| ApplyingToCollege | 4 / 4 | 2 / 2 |
| Cornell | 0 / 4 | 0 / 2 |
| college | 4 / 4 | 2 / 2 |

The 51 scientific slots are retained: 32 labeled arm cases can execute (16 constructed-splice and 16 account-ID-continuity proxy cases), two full unknown-truth natural histories can execute, and 17 slots are unfilled. The separate operational list contains six runnable omission-availability views and three unfilled slots; these reuse the same natural-history accounts and do not add independent scientific subjects.

The joint grouping audit covered 35,275 records and 384,533 candidate pairs under the unchanged 2,000,000-pair cap. Chronological histories were labeled exploratory development for grouping only. The independent graph check found no new paired split bridges. Account keys are proxies; metadata or observed text disjointness does not establish distinct people or complete independence.

All 21 helper/guard tests and five independent prepared-artifact tests passed. The latter re-read all three archives to verify complete available selected histories and preserved text/IDs/timestamps/statuses, and checked each fixed omission, surviving truth index, union-median cut, confirmation component exclusion and input hash. Preparation took 104.15 seconds with 1,778,116 KiB peak RSS under a 3 GiB address-space ceiling.

The failed initial launch receipt is preserved. That run stopped before the joint audit because the runpy wrapper could not import a neighboring script; ordinary Python execution fixed the launch. After the successful run, only the approved pool-completeness and source-freeze read guards were added. The exact executed script, final driver hashes and guard-only diff are retained in operational receipts.

Present source prose and actual timestamp instants are unchanged. Deleted/removed/unavailable bodies are null in the strict input contract; original archive rows and private source-text hashes/line locators preserve their construction facts. Complete means all available comments in these three archives, not complete Reddit/account history.

No scoring or output-grid calculation ran. The driver requires the final root scoring-freeze receipt and verifies every frozen protocol/script/input file before one-case execution.
