# Paired candidate content audit

The registered selection audit completed on all 33,190 eligible candidate records from 36 account keys. The input SHA256 is `b498a3f7cd9c11c05abc8cc998e37d02a12a535edeba48a4dc13ee4a2eee78a6`. No style distances or confirmation comparison scores were computed.

The audit observed 358,744 candidate pairs against the fixed 2,000,000-pair cap and performed 7,454 exact shingle-set comparisons after the shared-shingle guard. It reported 57 exact groups and 2,474 near relations. The specified three-account 15-word template and recognized-quote checks reported zero relations. Those zero counts concern these mechanical checks over this supplied pool; they do not establish absence of quotation, templates or other shared content.

All original IDs occur in exactly one of 32,878 completed graph components, including 32,756 singleton components. Six records lie in components spanning partitions: two development, one evaluation and three confirmation. The paired adapter removes all six symmetrically, alongside its separate thread exclusions. Account/thread restrictions and final sample adequacy remain separate checks.

Most matched material is within an account key: the near relation inventory contains 2,468 same-account and six cross-account pair memberships; three near pairs cross partitions. These are descriptive relation counts, not authorship or automation labels. Exact groups can contain multiple records, so their pair-membership totals are not directly comparable to their group count.

The actual run took 61.586 seconds and peaked at 1,651,692 KiB resident memory, with networking disabled by the Linux seccomp runner and a 3 GiB process address-space ceiling. It exited successfully with empty stderr. Resource ceilings and operational timing are receipts, separate from analytical output. See `logs/paired-leakage-audit.receipt.json` and its raw stdout/stderr logs.

The synthetic gate passed 28 tests, including hand-checkable threshold boundaries, an independent all-pairs set oracle, transitive components, symmetric purges, paragraph/exclusion boundaries, quote source maps, input permutations, cap exhaustion and memory-failure reporting. The initial 25-test receipt and subsequent successful gate receipts remain preserved.

An independent verifier rebuilt the exported graph without importing the audit implementation, checked every candidate ID's unique component membership and all six exclusions, and recomputed all 57 exact groups and all 2,474 near relations from fresh frozen preprocessing. It used rational-number threshold comparisons for the near-match oracle. It also checked the public aggregate for source prose. This passed in 22.502 seconds with 369,512 KiB peak resident memory; see `review/paired-leakage-independent-check.json` and `logs/paired-leakage-independent-check.receipt.json`. Large-pool nonmatching pairs were not independently exhaustively enumerated; the synthetic all-pairs oracle tests candidate completeness on bounded constructed inputs.

This graph covers the paired candidate pool only. A later joint graph with chronological candidates can reveal new paths or newly qualifying three-account templates. Its additional cross-partition relationships must be reported before claiming separation across the whole study. A source-record component is evidence about observed text relationships under a fixed rule, not independent natural persons, causes, unmarked quotations or unseen material.

The subsequent joint graph was checked on all 35,275 original records (the paired pool plus 2,085 records from eight chronological source accounts). It introduced **zero new paired cross-partition relationship pairs or bridge components** and retained exactly the initial six paired exclusions. This resolves the observed bridge concern for this completed joint pool only. The chronological inventory still reports 51 threads shared by multiple chronological authors and one related component containing multiple chronological authors; it remains exploratory, not an independently held-out study.

The audit's near-index implementation prefilters records having fewer than five distinct five-token shingles, because they cannot meet the required five-shared-shingle match guard. An independent broad index admitted these records while retaining the 20-word eligibility rule. There were six such records in both paired and joint populations, but they added **zero candidate pairs**. The independently recomputed broad and implemented counts were exactly equal: 358,744 paired and 384,533 joint. No cap or matching rule was changed. The joint index check passed in 45.654 seconds with 989,184 KiB peak memory under the same 3 GiB ceiling; see `review/audit-cap-and-joint-bridges.json` and `logs/audit-cap-and-joint-bridges.receipt.json`.

The original `prepared/paired` audit binds the same candidate bytes reproduced in `prepared/paired-registered`; the latter is the authoritative final paired preparation. The replay's saved-plan provenance difference is recorded separately in `inventory/paired-registered-replay.json`; the earlier audit and receipts are preserved.
