# Registered independent preparation and result checks

These checks are evaluation-only and must have tested implementation identities in the final pre-score freeze. They neither choose a favorable method/result nor authorize changing frozen cohort membership or source records. Preserve raw successful and failed receipts; a stored check is not a fresh rerun.

The independent preparation checker uses the fixed private candidate pool, registered audit result, exclusions, immutable final cohort and all360preparedbatches. Recompute source membership and original-record equality; exactly60distinctaccounts/30blocks/3strata; whole-record midpoint prefixes meeting2000words/eightrecords; account-hash selection; exact rational pairing-cost minimum and ties; allcellstatistics; all four independent omission rules; all16pairidentities; source/group memberships and connected resampling units. It must not import selection/preparation helpers as its answer oracle. Supplied original source lines and their stored byte digests remain independently verifiable; no historical prose inspection is needed. Run under4GiB address space,600seconds, at most100,000 candidate records and100MiB new metadata/check output. Fail on missing/incomplete input, never silently reduce validation.

The raw-distance checker selects two smallest SHA256("ahas-pilot3-raw-distance-check-v1:"+pair_id) IDs in each stratum/method/arm before reading observed evaluator values or qualification. This fixes72case selections (144requested sides), with at most48distinct prepared unit snapshots because the same planned pairs recur across methods. Cache only byte-identical inputs. At most300,000retainedwords across distinct preprocessed units,15,000record-preprocessingcalls,600seconds and4GiB address space are allowed. Independently construct character n4 counts for retained prose and function mask and their cosine arithmetic, and function-word counts/JS arithmetic. The unchanged frozen preprocessor supplies declared segments/views, but evaluator aggregate metrics and comparison-distance routines are not an oracle. Synthetic tests fix tolerances before scores. Compare raw-distance availability/value and qualification separately; an abstained raw distance remains abstained. No additional raw cases may be selected for favorable agreement.

The independent result checker consumes only the complete sanitized5760case rows,960unit/arm rows, dependency map and analysis outputs. It must not import the analysis or study-math implementation to reproduce answers. Independently reconstruct pair identities and score-orderings, allfouranchorcomplete block means, available-anchor secondary results, within/cross paired changes, stress outcomes, class counts, qualification and reason counts, pairwise tied AUC, tied-group noninterpolated AP, omission intersections and planned-stratum macro missingness. Independently regenerate the fixed10,000cluster-bootstrap draw identities and verify intervals/reasons with exact rational ordering arithmetic and type7percentiles. Preserve dependencies across allmethods/arms/anchors and any cross-stratum cluster. Bounds:600seconds,4GiB address space and20MiB check outputs. No source prose, preprocessing, evaluator scoring or new data selection enters this check.

The full first-sorted-stratum replay additionally runs120frozenbatches (allthree methods and four arms across allten blocks) under the alternate registered hash seed/timezone and relocated equivalent inputs. Compare all canonical evaluator artifacts byte-for-byte, excluding only operational runtime receipts. Report each command/exit, any failure and all mismatches. Final reporting must distinguish independent arithmetic checks, canonical replay, source fidelity and observational limitations; none proves human identity, corpus representativeness or independence from unobserved content.
