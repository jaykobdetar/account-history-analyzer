# Score-free amendment 04: preserve and correct the metadata-count ceiling failure

The first census-04 invocation failed to launch its Python target through the offline runner; its exit-1 receipt is preserved. The corrected invocation ran for 30.340 seconds, peaking at 277.5 MiB, completed the math archive's 1,534,925 rows, and failed at the 10,000-row limit checkpoint when combined rows reached 2,010,000. It had not completed the metadata pass and had made zero preprocessing or scoring calls. It is a resource-limited incomplete census, not a measured math/learnmath eligibility deficit. Its output, code, plans and both failed receipts remain unchanged.

No more archives will be acquired. For this same, already downloaded and hash-bound pair only, permit one corrected census with a maximum of 3,000,000 source rows per pass. The 2,000,000,000 decompressed-byte ceiling, two-pass rule, 500,000 preprocessing-call ceiling, 1,800-second time ceiling, 4 GiB address space and 500 MiB private-output bound remain unchanged. No new source, date scheme, selection criterion, guard or production limit is introduced. All global targets remain 60 accounts, 30 blocks and three 20-account strata. The previous failed metadata pass remains additional observed work, not silently counted as part of this new two-pass budget.

This correction follows observed file density and ample actual time/memory headroom, before observing any eligibility or style result from these two archives. Preserve this as an explicit pre-score resource amendment; it does not retroactively make census-04 complete. If this corrected attempt exhausts a declared limit, report that specific obstacle with the evidence then available instead of raising limits again. The deterministic pair calendar refinement and fixed-three-stratum joint check from amendment 03 remain conditional on a completed census. No distances may run until both original study gates pass.
