# Frozen score-free Gate B selection and candidate preparation rules

Registered before candidate assembly, contamination outcomes or any new style score. Gate A calendar-05 supports all60accounts/30blocks across the three fixed pair/date sets in candidate_preparation_plan.json. That plan hashes the actual eligibility metadata, capacity files, fixed Gate A report, mandatory exclusions and preparation helpers. The preceding scoring draft and all failed/amended censuses remain preserved. This document registers selection rules, not a passed contamination gate or completed scoring registration.

Candidate pool: include accounts from the three verified four-cell capacity sets. Maximize globally disjoint even account quotas up to40 per stratum, then balanced ascending quotas, then sorted stratum quotas. Assign accounts by deterministic residual-network flow with source identities ordered by SHA256("ahas-pilot3-account-rank-v1:" + casefolded identity). This is the exact tested quota algorithm applied to hashed identities. All eligible candidates considered and excluded-from-pool identities are privately recoverable; the candidate capacity witness is not the final cohort. Require at least20 candidate accounts in every stratum. This bounded pool is fixed before content/thread audit outcomes and will not be expanded in response to them.

In each candidate account/community/period cell, sort eligible original records by absolute integer UTC distance from the fixed period midpoint, then original timestamp, then source ID. Select whole records until both3,000words and eight records are reached. If only2,000–2,999words are available in at least eight records, include the entire eligible cell. An overshooting whole record is retained; no cutting, paraphrase, replacement or invented date. The3,000word audit buffer is not a scored budget. Enforce aggregate100,000record/2,000,000word/256MiB candidate-pool ceilings rather than assuming a nominal target is a hard size bound. A limit failure preserves the attempted pool, not a quietly trimmed subset.

Assemble this pool with one separately declared original-source pass over exactly the six selected community archives: at most6,000,000rows,3,000,000,000 decompressed bytes,30minutes and4GiB address space. Eligibility metadata has a1,200,000row ceiling. No preprocessing or scoring occurs in this extraction. Original source lines must match the prior census's per-record byte digests; preserve these lines privately and verify archive/utterance identities and row counts. AHAS conversion preserves original body, ID, timestamp, community, user identity, thread and parent metadata. Language remains an explicitly stated corpus-level assumption.

Apply the separately frozen Gate B audit rules and unchanged registered historical exact/near/template/blockquote definitions before final cohort selection. Remove candidate records when their content component or source thread crosses candidate cells or historical/protected boundaries. Audit all known available historical material plus explicit submission-title protection; record unavailable content limits. Missing required grouping or an exhausted audit cannot pass. Final cells may only draw from this fixed audited pool; no outside-pool refill is permitted.

After the audit, require all four surviving cells to support2,000words and eight records. Use the same hash-ordered quota allocator with cap20 to choose exactly20accounts per stratum and fail if any quota cannot be filled. Choose the full2,000word/eightrecord whole-record midpoint prefixes from audited survivors. Record actual words, counts, target overshoot, longest-record share, nearest-rank length quantiles, date endpoints, median timestamps and early/late gaps.

For each20account stratum, use the tested exhaustive subset dynamic program to minimize the additive pairing cost over allfourcells: absolute median-time difference divided by the period duration, plus absolute retained-word difference/2000, plus absolute record-count difference/8. Arithmetic is rational; tie-break by sorted source-identity hash pairs. Record which accounts had prior capacity-only exposure. Audit and preserve block dependence; never count repeated anchors or omission subsets as independent subjects.

Freeze final cohort, cells, account pairs, observed group provenance and unit membership before evaluating. Apply exactly full, nested hash75, nested hash50 and chronological-middle-half removal with the previously fixed salt, rounding and no-refill rules. Source text and timestamps are unchanged. No final replacement or refill may follow qualification failure or results. Separate full scoring registration must additionally bind methods, metrics, omissions, output batches, independent checks, all input snapshots and execution environment before the first new distance.
