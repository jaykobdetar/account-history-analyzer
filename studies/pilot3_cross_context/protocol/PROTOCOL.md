# Registered cross-context paired writing-comparison protocol

**Registered before any new style score.** The protocol is frozen for preparation and export; scoring additionally requires the hash-bound complete input/execution freeze and passing independent checks. No new score has been computed at this registration. Previous drafts, census failures, amendments, audit and selection receipts remain unchanged.

## Baseline and empirical scope

Use the installed, noneditable AHAS1.0.4 package built from reviewed repository commit `ea41d82ecc3f6585a7dc2bca92ede34740f0b62d`. Verified implementation fingerprint: `bfc989028bf2b47c506d1ba501287d4e362aadc25ca5c731c41e4b27a336e179`. Verified default analytical configuration hash: `8fd0239fe2f87c9f1506786ac36099fe996e00cc6e021b3ecc67fbb65cd2d925`. The isolated installation, every packaged byte and dependency identity are documented in `environment/`. The older workspace's stale editable-package metadata was explained and not used for study scoring. No production source, tokenizer, parser, view, function-word vocabulary, distance, sample guard, window, penalty, optimizer, resource configuration or classifier is changed. No threshold or reference is fitted.

The question is whether different-source-account late writing has greater distance than same-source-account late writing when both undergo the same community change. Labels are source-account identity proxies, not verified human authorship. This does not establish human/bot identity, account takeover, whether one person controls several accounts, cross-platform accuracy, full-stream boundary localization, or whole-suite accuracy.

## Fixed sources, dates, cohort and selection history

The bounded availability process is fully described in `review/gate-a-final/FEASIBILITY.md`, source and capacity tables, the three completed census directories, both calendar refinements, acquisition receipts and four numbered score-free amendments. The original three-archive result8accounts/4blocks, expanded four-scheme result48/24, and eight-archive calendar result52/26 remain failures of their original rules. The final mathematics archive intake added an eligible third stratum under the same2000word/eightrecord four-cell targets. Its original metadata-limit failure and separately bounded correction remain preserved; no further acquisition is planned.

Ten official ConvoKit archives total918,682,477 compressed bytes and5,522,738 counted rows. The six sources used in the final cohort are AskAcademia, GradSchool, AskPhysics, Physics, learnmath and math. These are authorized local historical samples, not complete verified account histories. Original prose/account maps/private exports remain outside the public study directory. Public redistribution rights have not been established. English is a corpus assumption; per-record verified-English labels and missing edit history are not invented.

| Public stratum | Fixed community pair (X / Y) | Early period | Late period |
|---|---|---|---|
|stratum-01|AskAcademia / GradSchool|[2008-01-01,2015-10-06)|[2015-10-06,2018-11-01)|
|stratum-02|AskPhysics / Physics|[2008-01-01,2015-09-20)|[2015-09-20,2018-11-01)|
|stratum-03|learnmath / math|[2008-01-01,2016-10-05)|[2016-10-05,2018-11-01)|

All boundaries are UTC midnight. Each stratum uses the same shared periods for both communities and every account. They represent related forum scopes—academic careers/graduate-school discussion, physics questions/general physics, mathematics learning/general mathematics—not verified historical topic labels. All final strata extend beyond the original three education communities. This selection remains a convenience sample of relatively prolific surviving accounts, within one platform/provider and historical period; no representative all-Reddit accuracy or power guarantee is claimed.

The unchanged target is60 previously unscored accounts,20 per stratum,30 disjoint two-account blocks and240 full account/community/period cells. The mandatory exclusion manifest covers57 historical identities:12 pilot1,24 scored paired pilot2,12 protected confirmation reserve,8 chronological pilot2 and1 private export. The additional145 prior capacity-only inspected identities were disclosed separately rather than falsely labeled scored. None of those145 is in the final selected cohort. No protected reserve score or feature inspection informed the design; its text entered only the frozen automatic protection audit.

`GATE_B_SELECTION_RULES.md`, `candidate_preparation_plan_v2.json`, and their freezes define the candidate and final selection. Candidate quotas38/36/40 were chosen by the prespecified hash-ordered exact disjoint allocation with cap40. Each cell supplied a whole-record midpoint prefix with a3000word audit-buffer target, at least8 records, or all available writing when only2000–2999words existed. No audit-driven pool expansion or source refill was permitted. The frozen pool contained114accounts,456cells,15,243records and1,362,098retainedwords. Original source lines were preserved privately and matched their census byte digests.

The registered audit in `GATE_B_AUDIT_RULES.md` completed on15,243new records plus38,884 historical bodies and53 supplied submission-title protection nodes. It independently counted606,878 broad five-shingle candidate pairs, of which606,874 met the engine candidate definition; both were below the unchanged2,000,000 ceiling. The frozen engine reported90exact,2,807near,1template and0recognized-quotation relations across this entire protection population. These are audit counts, not stylistic results. Symmetric content and thread cross-cell rules purged2,082new records, leaving13,161. All required thread IDs were present. Seventeen unavailable historical bodies have unknown hidden relationships; only supplied available content/grouping was audited, and `independence_scope_complete` remains false. Unmarked quotations, shorter matches, paraphrases and unsupplied content remain outside this check.

After filtering,23academic,23physics and24mathematics accounts retained allfour required2000word/eightrecord cells. The registered final hash allocator selected exactly20 per stratum. `final-selection-freeze.json` binds the inputs/rules/code; private `cohort-01/cohort.json` has SHA256 `315e70c21d4d2bc96620aec781f9a9d97c94dc68a3d0ca77e581e0b4d5c6f0bb`. The final cohort has5,773distinct original comments and502,546eligible words. All240fullcells meet both targets. Actual cell words range2000–2844 and record counts8–50; budgets are whole-record targets, not identical lengths. Per-cell overshoot, longest-record share, nearest-rank length quantiles, date endpoints, medians and early/late gaps are retained. Some single records supply a large share of a cell; matching does not establish identical context or timing.

Within each stratum, the tested exact subset dynamic program minimizes the sum overfourcells of absolute median-time difference/period duration + absolute retained-word difference/2000 + absolute record-count difference/8. Arithmetic is rational and ties use sorted source-identity hash pairs. Record selection uses distance from the calendar midpoint, then original timestamp and ID, with whole-record stopping once2000words and8records are met. Final membership, pairs and cells are fixed. No replacement, post-freeze refill, corpus expansion or rebalancing may follow an abstention or result.

The surviving thread/content graph contains no observed cross-block relation under the registered definitions, yielding30 resampling units,10 per stratum. The grouping code nevertheless merges any detected connected blocks and the independent checker verifies these assignments. Within-block repeated anchors and all omission/method repeats remain dependent.

## Registered audit and export interpretation

The existing evaluator audits overlap between development and evaluation splits. These batches intentionally contain evaluation observations only, so its split-audit status remains `not_auditable` because the development split is absent. Do not relabel that status as a passed audit. The separate full protection-pool Gate B audit and independent graph/source checks supply the study's historical/cross-cell checks. Export every available original source-document, thread and content-component group for the actual retained omission rows, and preserve author/related-sample dependence. Empty omission units receive no fabricated source groups.

An unexecuted export prototype omitted source-document groups. A separate export adapter adds that metadata before real export; the already executed final selector and its immutable cohort remain unchanged. This is evaluation preparation, not a numerical engine repair. Both implementations and synthetic test receipts remain available.

## Comparisons and omissions

Each block has A/X, A/Y, B/X and B/Y in early and late periods. For each of the
four early anchors, compare its late counterpart in all four fixed categories:
same account/same community, different account/same community, same account/
different community, and different account/different community. This gives
16 comparisons per block per method/arm. Repeated anchors retain dependence;
the two sides of any one comparison must contain disjoint original record IDs.

Primary method: `cosine_distance_v1`, `retained_prose`, n=4. Secondary methods:
`cosine_distance_v1`, `function_mask_v1`, n=4; and `function_word_js_v1`,
`lexical_tokens`, n=null. All planned comparisons use all three. The evaluator's
`same_author`/`different_author` enums remain required implementation labels;
`label_definition` and all prose explicitly mean same-source-account and
different-source-account proxies. No combined score or fitted decision threshold.

Four arms, with no refill or changed record text/timestamps:

- Full prepared cell.
- Hash75: retain records whose SHA256 hash integer is below three quarters of
  `2**256`.
- Hash50: retain records below one half of the same hash space; this is nested
  within hash75. Salt: `ahas-pilot3-omission-v1`, joined to original record ID with
  a colon and encoded in UTF-8.
- Middle50: sort by original timestamp and ID; delete indices from `floor(n/4)`
  through `floor(3*n/4)-1`. Empty and undersized results remain in denominators.

## Outcomes and uncertainty

For each anchor, the primary cross-context outcome compares different-account
distance with same-account distance in the other community: 1 if greater, 0 if
less, 1/2 on an exact stored-score tie. Either distance unavailable makes the
anchor unavailable. Average the four anchors only if all four are available to
obtain a complete block score. Show available-anchor summaries separately.

Publish stratum-specific equal-block means first. Any equal-stratum macro mean
requires all three planned strata; do not silently renormalize missing strata.
Compute within-community ordering and cross-minus-within change on the same
complete blocks. Keep every per-anchor outcome and raw distance. Report the
separate harder stress contrast: different-account/same-community distance versus
same-account/different-community distance.

Secondary ROC-AUC uses pairwise rankings with one half for exact ties;
noninterpolated AP adds recall increments at complete tied-score groups.
Different-source-account is the positive proxy. Publish both class counts,
qualification denominators and unavailable reasons by method, stratum, context
and arm. Pooled differently scaled distances are not the primary evidence.

Preserve account blocks across every method/arm/anchor. Merge blocks linked by
any residual audited shared thread/content component into resampling units.
Disclose each stratum's effective independent-unit count. With fewer than five
units, report descriptive estimates and unavailable uncertainty. Otherwise use
10,000 deterministic cluster-bootstrap resamples, fixed SHA256 counter generator
`sha256-json-counter-rejection-v1`, seed
`ahas-pilot3-cross-context-block-bootstrap-v1`, exact arithmetic for orderings,
and type-7 interpolated 2.5th/97.5th percentiles. Use one global draw stream over all audited resampling units, preserving every associated block and all method/arm/anchor observations. Each stratum statistic uses its contributing blocks from these same draws; stratum draw counts may vary. This retains any cross-stratum dependence instead of splitting a connected unit. A statistic with fewer than five contributing independent units receives no interval. If any of its10,000 bootstrap replicates is missing, withhold that interval and report the missing count; never discard or reweight missing replicates. Resample the same units for paired method and arm comparisons. Freeze/export draw identity hashes, generator version and exact arithmetic. Point macro means require all three planned strata; AUC/AP and macro summaries remain descriptive. These
intervals describe only the selected-corpus resampling model.

For each deletion arm report planned units, nonempty units, word/record-qualified
units, method-qualified pairs, complete primary blocks, all abstention reasons
and observed remaining words. Compare separation changes on the intersection
that qualifies in both arms alongside coverage for all planned cases. Attrition
cannot turn a selected-case improvement into an unconditional improvement.

## Execution and final input freeze

Use the existing offline paired evaluator. Batch one account block per method/
arm: eight subset-only snapshots and 16 pairs. Validate all source bytes plus
dataset bytes against 50 MiB, all unique records against 10,000, and every text
against 200,000 codepoints; fail rather than trimming. The largest target design
has 360 batches and 5,760 method/arm comparison rows, representing 60 accounts.
At most two processes run concurrently. Each evaluator process inherits a4GiB address-space operational ceiling and a300-second external wall timeout; each complete main or replay execution has a7,200-second dispatch budget. Every timeout or unstarted registered batch remains a reported failure. These operational limits do not alter production sample guards, input limits, formulas or qualification behavior. Export compact evaluator outputs; no
chronological full-report runs are part of this study. Preserve wall time, peak
memory, words, byte sizes, exit codes and failed attempts for every command.

Before the first style score, hash protocol, inventory, exclusions, audit rules,
all cohort/cell/record/group manifests, pair tables, omissions, scoring/analysis/
check code, batches and environment. Preserve this draft and every amendment.
Do not label a post-score amendment as preregistered confirmation.

Independently check source membership and byte fidelity, whole-record budgets,
omissions, pair identities, leakage/grouping and metric arithmetic. Verify a
preselected distance sample by direct feature/distance arithmetic independent
of evaluator metric aggregation. Replay a complete stratum for all three methods
and four arms in another process with a different hash seed, timezone and relocated
equivalent inputs, comparing canonical evaluator bytes. Operational paths and
timing may differ. A saved receipt is not a fresh replay.

If Gate A fails, stop without new distances and report the actual census,
unfilled quotas, authorized-source limitations and smallest concrete additional
data request supported by the evidence. If any repair is needed later, preserve
the frozen-baseline failure, separately review a narrow patch and identify every
affected rerun. Neither a positive result nor an accuracy target defines success.

## Exact execution and independent verification freeze

Use the frozen noneditable installation through the existing socket-denial offline runner. Main processes set `PYTHONHASHSEED=0`, `TZ=UTC`, `LC_ALL=C.UTF-8`; replay processes set `PYTHONHASHSEED=73129`, `TZ=Pacific/Honolulu`, `LC_ALL=C.UTF-8`. Replay every registered method/arm/block in the first sorted stratum, AskAcademia/GradSchool, in a fresh process using relocated byte-equivalent inputs. Compare canonical evaluator artifacts exactly; runtime/path receipts may differ. The full design has360 main batches/5760rows and120 replay batches/1920rows. Log each actual command, exit, timing, peak memory, distinct input/sample words/records and artifact bytes; do not replace a fresh rerun with a stored successful receipt.

Before the first distance, the final scoring freeze must bind this protocol, all input files/manifests and batch index, source/exclusion/audit/cohort receipts, selection/pairing/group maps, method/omission rules, analysis and independent-check code/tests, baseline identity, and execution settings. Any missing prerequisite blocks scoring. Preserve every preceding draft, amendment and failed command. Synthetic-only development checks may precede registration; real scores may not. Any later defect requires preserved failure evidence and a separately identified reviewed repair/re-execution, not silent baseline movement.

Independent preparation checks recompute cohort membership, surviving capacity, source bytes, full midpoint prefixes, minimum matching cost, cell statistics, omission subsets, pair identities and group dependencies. Independent source checks already compared100records per completed expanded/math intake using an alternate frozen-preprocessor word-count view; both found zero mismatches. These checks do not prove source completeness or language/human identity.

For raw-distance verification select exactly two comparison IDs with the smallest SHA256("ahas-pilot3-raw-distance-check-v1:" + pair_id) in each stratum/method/arm, independent of score or availability:72case selections. Compute retained-prose and function-mask n4 character counts/cosine, and function-word JS arithmetic independently of evaluator metric aggregation, using the unchanged frozen preprocessing rules and views. Keep raw-distance validity separate from qualification; do not turn an abstained raw value into an available score. The checker runs only after scoring with a pre-score bound implementation, preserving all numeric tolerances and missingness rules established in synthetic tests.

Independent result checks must recompute all per-anchor/block orderings, missingness denominators, context class counts, AUC/AP arithmetic, paired changes and omission intersections from retained case rows, rather than accepting stored summaries. Keep allplannedstrata and fixedprimarymethod. The final decision report answers the context-discrimination and omission questions using observed results, including negative/inconclusive outcomes and inadequate uncertainty when applicable. Neither a high score nor a new detector is required for completion.
